#include <stdio.h>
#include<conio.h>
int main()
{
    char s[1000], i;

    printf("Enter a string: ");
    scanf("%s","%d" s)

    for(i = 0; s[i] != '\0'; ++i);

    printf("Length of string: %d", i);
    return 0;
}
