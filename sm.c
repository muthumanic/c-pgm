#include<stdio.h>
#include<string.h>
int main()
{
	int num,len,p,i;
	char str[100];
    scanf("%s",str);
	scanf("%d",&add);
	len=strlen(str);
	p=l-num;
	for(i=p;i<l;i++)
	{
		printf("%c",string[i]);
	}
	return0;	
}
